# MineLauncher - module-info approach
# This project intentionally uses --add-opens via build.gradle rather than
# a module-info.java to remain compatible with all JavaFX library reflection patterns.
# The application runs on the classpath (non-modular) for maximum compatibility.
